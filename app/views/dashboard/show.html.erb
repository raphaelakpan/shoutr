<h1>Dashboard</h1>

<%= form_for @shout do |form| %>
  <%= form.fields_for :content do |content_form| %>
    <%= content_form.text_field :body, placeholder: "What's on your mind?", required: true %>
  <% end %>
  <%= form.submit "Shout" %>
<% end %>

<%= render @shouts %>
